# Copy this to credentials.yaml, fill in your values, and run: chmod 600 credentials.yaml
# This file is gitignored.

resy:
  api_key: "VbWk7s3L4KiK5fzlO7JD3Q5EYolJI7n5"   # public Resy app key (same for everyone)
  auth_token: "<extracted from browser network tab after login>"
  payment_id: 12345678                             # from: python manage.py lookup-payment-id
  account_email: "you@example.com"                 # used by refresh_auth.py

sevenrooms:
  auth_token: ""   # captured via DevTools after investigating booking flow (Phase 5)

ntfy:
  topic: "your-topic-name"   # e.g. "ressniper-oscar" — create at ntfy.sh (free, no account needed)
  # server: "https://ntfy.sh"  # uncomment and change if self-hosting ntfy
